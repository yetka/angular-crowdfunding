import { HasGoalBeenReachedPipe } from './has-goal-been-reached.pipe';

describe('HasGoalBeenReachedPipe', () => {
  it('create an instance', () => {
    const pipe = new HasGoalBeenReachedPipe();
    expect(pipe).toBeTruthy();
  });
});
